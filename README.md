# SpringMVC-ModelAttribute-Hibernate
A Spring MVC project demonstrating form handling using @ModelAttribute and persisting data to a MySQL database using Hibernate (JPA) with Java-based configuration.
